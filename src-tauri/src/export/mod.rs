//! Export logic: write transcript to TXT and DOCX.
//! Used by Tauri commands when the user exports from the UI.

// TODO: implement export_to_txt(path, content)
// TODO: implement export_to_docx(path, content) — e.g. via docx crate or similar
// Both will be called from commands with paths from the frontend (save dialog).
